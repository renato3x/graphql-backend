#import User, UserFilter, UserLoginInput, Profile, ProfileFilter from 'user.graphql'

type Query {
	users: [User]
	user(filters: UserFilter!): User
	profiles: [Profile]
	profile(filters: ProfileFilter!): Profile
	login(data: UserLoginInput): User
}